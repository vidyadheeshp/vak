__pycache__/
*.py[cod]
.venv/
venv/
.vscode/
.idea/
.pytest_cache/
*.egg-info/
build/
*.exe
*.o

# generated by the native back end
*.orig
docs/*.data
# emitted C — the .vak source is the source
examples/*.c
स्वयंसिद्धिः/*.c

_wasm/

# packaging
dist/
*.whl
*.tar.gz

# benchmark output — machine-specific, regenerate with bench/benchmark.py
bench/results.json

# the packaged editor extension — built by the release workflow
vscode-vak/*.vsix
